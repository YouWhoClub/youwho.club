import { Box, Typography } from "@mui/material";

const ChargeWallet = () => {
    return (
        <Box>
            <Typography sx={{ color: 'primary.text' }}>Charge Wallet</Typography>
        </Box>
    );
}

export default ChargeWallet;