import { Box, Typography } from "@mui/material";

const TransferGift = () => {
    return (
        <Box>
            <Typography sx={{ color: 'primary.text' }}>Transfer NFT Gifts</Typography>
        </Box>
    );
}

export default TransferGift;