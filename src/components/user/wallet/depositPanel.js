import { Box, Typography } from "@mui/material";

const DepositPanel = () => {
    return (
        <Box>
            <Typography sx={{ color: 'primary.text' }}>Deposit Panel</Typography>
        </Box>
    );
}

export default DepositPanel;